<% provide(:title, 'All users') %>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.name %></td>
      <td><%= user.email %></td>
      <%= link_to "delete", admin_user_path(user), method: :delete, data: { confirm: "You sure?" } %>
    </tr>
    <br>
<% end %>
